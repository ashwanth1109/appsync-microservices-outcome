#set($payload = "query GetOrder {
    getOrder(id: ""$ctx.arguments.id"") {
        id
        userId
        product
        quantity
    }
}")
{
    "version": "2018-05-29",
    "method": "POST",
    "resourcePath": "/graphql",
    "params": {
        "headers": {
            "Content-Type": "application/json",
            "authorization": "$context.request.headers.authorization",
        },
        "body": {
            "query": "$util.escapeJavaScript($payload)"
        }
    }
}